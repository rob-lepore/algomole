#include "mat3D.h"
/*
using namespace am;

template <typename T>
Mat3D<T>::Mat3D(size_t width, size_t height, size_t depth) {
	m_w = width;
	m_h = height;
	m_d = depth;
	m_data.reserve(m_w * m_h * m_d);
}

template <typename T>
T& Mat3D<T>::at(size_t x, size_t y, size_t z) {
	return m_data[(z * m_h * m_w) + (y * m_w) + x];
}

template <typename T>
size_t Mat3D<T>::width() {
	return m_w;
}

template <typename T>
size_t Mat3D<T>::height() {
	return m_h;
}

template <typename T>
size_t Mat3D<T>::depth() {
	return m_d;
}

template <typename T>
size_t Mat3D<T>::size() {
	return m_w * m_h * m_d;
}

*/